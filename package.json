{
  "name": "@djie/eslint-config-monorepo",
  "version": "1.0.1",
  "private": true,
  "packageManager": "pnpm@7.3.0",
  "license": "MIT",
  "scripts": {
    "lint": "eslint .",
    "prepare": "pnpm -r run stub",
    "release": "bumpp package.json packages/*/package.json --commit --push --tag && pnpm -r publish --access public"
  },
  "devDependencies": {
    "@djie/eslint-config": "*",
    "bumpp": "^7.1.1",
    "eslint": "^8.16.0",
    "eslint-plugin-antfu": "^0.25.1",
    "typescript": "^4.7.2"
  }
}
